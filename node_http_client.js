var http = require('http');

console.log('Welcome to socket pool.');

if (!nitin) {
    var nitin = {};
}

(function() {
    if (!nitin['sockets']) {
        nitin.sockets = {};
    }
    
    var agent = new http.Agent();
    console.log(agent);

})();

console.log('===***===');

